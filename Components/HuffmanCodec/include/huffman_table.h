/**
 * huffman_table.h — PLACEHOLDER
 *
 * This file must be regenerated from your live log corpus before building
 * the HuffmanCodec component.
 *
 * Run (with the log service accessible on localhost:8080):
 *   python3 lightinator-log-service/scripts/huffman_analysis.py \
 *       --ngrams --emit \
 *       --out Components/HuffmanCodec/include/huffman_table.h
 *
 * The generated file will contain:
 *   NGRAM_TABLE, NGRAM_LEN, NGRAM_FIRST, NGRAM_LAST   — n-gram string pool
 *   HUFFMAN_TABLE[255]                                  — encode table
 *   HUFF_DECODE_COUNT/FIRST/GROUP_OFFSET/SYMBOLS       — decode tables
 *   NGRAM_COUNT, NGRAM_MAX_LEN, HUFFMAN_ALPHABET_SIZE, HUFF_MAX_CODE_BITS
 */
#error "huffman_table.h not generated yet. Run: python3 huffman_analysis.py --ngrams --emit --out huffman_table.h"
